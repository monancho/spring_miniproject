<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.monancho.board.dao.MemberDao">
    	
    	<insert id="MBRinsert" parameterType="com.monancho.board.dto.MemberDto">
    		INSERT INTO membertbl (membernum ,memberid, memberpw, membername )
    		VALUES(member_seq.nextval, #{memberid}, #{memberpw}, #{membername})
    	</insert>
   		
   		<select id="MBRcheckId" resultType="int">
   			SELECT COUNT(*) FROM membertbl WHERE memberid = #{param1}
   		</select>
   		<select id="MBRlogin" resultType="int">
   			SELECT COUNT(*) FROM membertbl WHERE memberid = #{memberid} AND memberpw = #{memberpw}
   		</select>
   		
   		<select id="MBRselectById" resultType="com.monancho.board.dto.MemberDto">
   			SELECT memberid, membername, memberdate 
   			FROM membertbl WHERE memberid = #{param1}
   		</select>
   		
   		<update id="MBRupdate">
   			UPDATE membertbl SET membername = #{membername} WHERE memberid = #{memberid}
   		</update>
   		
   		<delete id="MBRdelete">
   			DELETE FROM membertbl WHERE memberid = #{param1}
   		</delete>
    </mapper>